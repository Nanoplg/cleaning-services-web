(()=>{const bytes=new Uint8Array(window.__providerImageBytes||[]);const image=document.getElementById("providerPlatformImage");if(!image||!bytes.length)return;const url=URL.createObjectURL(new Blob([bytes],{type:"image/avif"}));image.src=url;image.addEventListener("load",()=>{image.classList.add("is-loaded");URL.revokeObjectURL(url)},{once:true});image.addEventListener("error",()=>{image.classList.add("is-error");URL.revokeObjectURL(url)},{once:true});})();